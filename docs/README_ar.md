🌐 [English](../README.md) | [한국어](README_ko.md) | [日本語](README_ja.md) | [中文](README_zh.md) | [Español](README_es.md) | [Français](README_fr.md) | [Deutsch](README_de.md) | [Русский](README_ru.md) | [हिन्दी](README_hi.md) | [العربية](README_ar.md)

<div align="center">

# ai-initializer

**أمر واحد يمنح أي وكيل ذكاء اصطناعي فهماً فورياً للمشروع.**

يفحص مشروعك ← يولّد `AGENTS.md` + سياق المعرفة/المهارات/الأدوار
← أي أداة ذكاء اصطناعي تبدأ العمل فوراً، في كل جلسة.

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](../LICENSE)

</div>

---

## جرّبه الآن

يتضمّن هذا المستودع ملفات `AGENTS.md` و`.ai-agents/` جاهزة مسبقاً كمثال عملي.
استنسخه وشغّل `ai-agency.sh` فوراً لترى كيف يعمل:

```bash
git clone https://github.com/itdar/agents-initializer.git
cd agents-initializer
./ai-agency.sh
```

```
=== AI Agent Sessions ===
Project: /home/user/agents-initializer
Found: 4 agent(s)

  1) [PM] ai-initializer                (bg: Warm Brown)
     Path: ./AGENTS.md
     Project orchestrator managing all sub-agents

  2) business — Business Planner         (bg: Navy)
     Path: business/AGENTS.md
     Business strategy, KPI tracking, go-to-market

  3) docs — Multi-language Docs          (bg: Forest)
     Path: docs/AGENTS.md
     Documentation and translation specialist

  4) planning — Technical Writer         (bg: Plum)
     Path: planning/AGENTS.md
     Specs, roadmaps, architecture documents

Select agent (number, or 'q' to quit): 2

=== AI Tool ===
  1) claude  (Claude Code CLI)
  2) codex   (OpenAI Codex CLI)
  3) print   (طباعة الموجّه فقط — للنسخ اليدوي)

Select tool (1-3): 1

→ الوكيل يقرأ AGENTS.md + يحمّل .ai-agents/context/ تلقائياً
→ جاهز للعمل فوراً!
```

---

## تطبيق على مشروعك

> **مهم:** انسخ `setup.sh` و`HOW_TO_AGENTS.md` إلى **دليل مشروعك الخاص** وشغّلهما من هناك.
> هذه الملفات تحلّل هيكل المشروع المستهدف — يجب أن تكون داخله.

```bash
# 1. انسخ الملفات إلى مشروعك
cp setup.sh HOW_TO_AGENTS.md /path/to/your-project/
cd /path/to/your-project

# 2. شغّل الإعداد التفاعلي (يختار أداة الذكاء الاصطناعي واللغة، ثم يولّد كل شيء تلقائياً)
./setup.sh

# 3. شغّل جلسة وكيل
./ai-agency.sh
```

هذا كل شيء. `setup.sh` يتولى اكتشاف الأدوات واختيار اللغة وتشغيل التوليد الكامل تلقائياً.

<details>
<summary><b>الإعداد اليدوي (بدون setup.sh)</b></summary>

```bash
cd /path/to/your-project

# الخيار A: بالإنجليزية (موصى به — تكلفة توكنز أقل، أداء أمثل للذكاء الاصطناعي)
claude --dangerously-skip-permissions --model claude-opus-4-6 \
  "Read HOW_TO_AGENTS.md and generate AGENTS.md tailored to this project"

# الخيار B: بلغتك
claude --dangerously-skip-permissions --model claude-opus-4-6 \
  "اقرأ HOW_TO_AGENTS.md وولّد AGENTS.md مخصصاً لهذا المشروع"

# ثم شغّل جلسات الوكلاء
./ai-agency.sh
```

</details>

> **تنبيه حول التوكنز:** الإعداد الأولي يحلّل المشروع كاملاً وقد يستهلك عشرات الآلاف من التوكنز. هذه تكلفة لمرة واحدة فقط — الجلسات اللاحقة تحمّل السياق المُعدّ مسبقاً فوراً.

---

## لماذا تحتاج إلى هذا؟

### المشكلة: الذكاء الاصطناعي يفقد ذاكرته في كل جلسة

```
 الجلسة 1               الجلسة 2               الجلسة 3
┌──────────┐         ┌──────────┐         ┌──────────┐
│ الذكاء   │         │ الذكاء   │         │ البدء    │
│ يقرأ     │  تنتهي  │ يقرأ     │  تنتهي  │ من       │
│ قاعدة    │ الجلسة  │ قاعدة    │ الجلسة  │ الصفر    │
│ الكود    │ ──────→ │ الكود    │ ──────→ │  مجدداً   │
│ (30 دقيقة│ فقدان   │ (30 دقيقة│ فقدان   │(30 دقيقة │
│ يبدأ     │الذاكرة! │ يبدأ     │الذاكرة! │ يبدأ     │
│ العمل    │         │ العمل    │         │ العمل    │
└──────────┘         └──────────┘         └──────────┘
```

تنسى وكلاء الذكاء الاصطناعي كل شيء عند انتهاء الجلسة. في كل مرة، تقضي وقتاً في فهم هيكل المشروع وتحليل واجهات API وتعلم الأعراف.

| المشكلة | العواقب |
|---|---|
| لا تعرف أعراف الفريق | تناقضات في أسلوب الكود |
| لا تعرف خريطة API الكاملة | تستكشف قاعدة الكود بالكامل في كل مرة (تكلفة +20%) |
| لا تعرف الإجراءات المحظورة | عمليات محفوفة بالمخاطر مثل الوصول المباشر لقاعدة بيانات الإنتاج |
| لا تعرف تبعيات الخدمات | آثار جانبية مغفولة |

### الحل: أنشئ مسبقاً "دماغاً" للذكاء الاصطناعي

```
 بداية الجلسة
┌──────────────────────────────────────────────────┐
│                                                  │
│  يقرأ AGENTS.md (تلقائياً)                        │
│       │                                          │
│       ▼                                          │
│  "أنا خبير الـ backend لهذه الخدمة"              │
│  "الأعراف: Conventional Commits، TypeScript      │
│   strict"                                        │
│  "المحظور: تعديل خدمات أخرى،                     │
│   ترميز الأسرار مباشرةً"                          │
│       │                                          │
│       ▼                                          │
│  يحمّل ملفات .ai-agents/context/ (5 ثوانٍ)         │
│  "تم فهم 20 API، 15 كياناً، 8 أحداث"              │
│       │                                          │
│       ▼                                          │
│  يبدأ العمل فوراً!                                │
│                                                  │
└──────────────────────────────────────────────────┘
```

**ai-initializer** يحل هذه المشكلة — ولِّد مرة واحدة، وأي أداة ذكاء اصطناعي ستفهم مشروعك على الفور.

---

## المبدأ الأساسي: بنية ثلاثية الطبقات

```
                  مشروعك
                     │
        ┌────────────┼────────────┐
        ▼            ▼            ▼

 ┌──────────┐  ┌──────────┐  ┌──────────┐
 │ AGENTS.md│  │.ai-agents│  │.ai-agents│
 │          │  │ /context/│  │ /skills/ │
 │ الهوية   │  │ المعرفة  │  │ السلوك   │
 │ "من      │  │ "ماذا    │  │ "كيف     │
 │  أنا؟"   │  │  أعرف؟"  │  │  أعمل؟"  │
 │          │  │          │  │          │
 │ + قواعد  │  │ + النطاق │  │ + النشر  │
 │  + صلاحيات│  │ + نماذج  │  │ + مراجعة│
 │ + مسارات │  │          │  │          │
 └──────────┘  └──────────┘  └──────────┘
  نقطة الدخول   مخزن الذاكرة  معايير سير العمل
```

### 1. AGENTS.md — "من أنا؟"

**ملف الهوية** للوكيل المنتشر في كل دليل.

```
project/
├── AGENTS.md                  ← PM: القائد الذي ينسق كل شيء
├── apps/
│   └── api/
│       └── AGENTS.md          ← خبير API: مسؤول عن هذه الخدمة فقط
├── infra/
│   ├── AGENTS.md              ← SRE: يدير كل البنية التحتية
│   └── monitoring/
│       └── AGENTS.md          ← متخصص المراقبة
└── configs/
    └── AGENTS.md              ← مدير الإعدادات
```

يعمل تماماً مثل **الهيكل التنظيمي للفريق**:
- يشرف PM على كل شيء ويوزع المهام
- كل عضو في الفريق يفهم منطقته بعمق فقط
- لا يتعاملون مباشرة مع عمل الفرق الأخرى — يطلبون ذلك

### 2. `.ai-agents/context/` — "ماذا أعرف؟"

مجلد تُنظَّم فيه **المعرفة الأساسية مسبقاً** حتى لا يضطر الذكاء الاصطناعي لقراءة الكود في كل مرة.

```
.ai-agents/context/
├── domain-overview.md     ← "هذه الخدمة تتعامل مع إدارة الطلبات..."
├── data-model.md          ← "توجد كيانات Order وPayment وDelivery..."
├── api-spec.json          ← "POST /orders، GET /orders/{id}، ..."
└── event-spec.json        ← "تنشر حدث order-created..."
```

**تشبيه:** وثائق التهيئة لموظف جديد. وثِّقها مرة واحدة، ولن يحتاج أحد لشرحها مجدداً.

### 3. `.ai-agents/skills/` — "كيف أعمل؟"

**أدلة سير عمل موحّدة** للمهام المتكررة.

```
.ai-agents/skills/
├── develop/SKILL.md       ← "تطوير المميزات: تحليل → تصميم → تنفيذ → اختبار → PR"
├── deploy/SKILL.md        ← "النشر: وسم → طلب → تحقق"
└── review/SKILL.md        ← "المراجعة: قائمة تحقق للأمان والأداء والاختبار"
```

**تشبيه:** دليل عمليات الفريق — يجعل الذكاء الاصطناعي يتبع قواعد مثل "تحقق من هذه القائمة قبل تقديم PR".

---

## ماذا تكتب وماذا لا تكتب

> ETH Zurich (2026.03): **تضمين المحتوى القابل للاستنتاج يقلل معدلات النجاح ويزيد التكلفة بنسبة +20%**

```
       اكتب هذا                        لا تكتب هذا
┌─────────────────────────┐     ┌─────────────────────────┐
│                         │     │                         │
│  "استخدم صيغة feat:     │     │  "الكود المصدري في      │
│   للـ commits"          │     │   مجلد src/"            │
│   الذكاء لا يستطيع       │     │  الذكاء يرى هذا بـ ls  │
│  استنتاجه               │     │                         │
│                         │     │                         │
│   "لا push مباشر         │     │  "React مبني على       │
│   إلى main"             │     │   المكونات"             │
│  قاعدة الفريق، غير      │     │  موجود في التوثيق       │
│  موجودة في الكود        │     │  الرسمي                 │
│                         │     │                         │
│  "يتطلب موافقة فريق QA  │     │  "هذا الملف 100 سطر"    │
│   قبل النشر"            │     │  الذكاء يستطيع قراءته   │
│  إجراء، غير قابل        │     │  مباشرة                 │
│   للاستنتاج              │     │                        │
│                         │     │                         │
└─────────────────────────┘     └─────────────────────────┘
     اكتب في AGENTS.md              لا تكتب!
```

**استثناء:** "الأشياء القابلة للاستنتاج ولكنها مكلفة جداً إذا فُعلت في كل مرة"

```
  مثال: قائمة API الكاملة (تتطلب قراءة 20 ملفاً)
  مثال: علاقات نموذج البيانات (منتشرة عبر 10 ملفات)
  مثال: علاقات الاستدعاء بين الخدمات (تتطلب فحص الكود + البنية التحتية)

  → نظِّم هذه مسبقاً في .ai-agents/context/!
  → في AGENTS.md، اكتب المسار فقط: "اذهب هنا للعثور عليه"
```

```
ضمِّن (غير قابل للاستنتاج)    .ai-agents/context/ (استنتاج مكلف)    استبعد (استنتاج رخيص)
────────────────────────       ──────────────────────────────────    ─────────────────────
أعراف الفريق                   خريطة API الكاملة                    هيكل الدلائل
الإجراءات المحظورة             علاقات نموذج البيانات               محتويات الملف الواحد
صيغ PR/commit                  مواصفات نشر/استقبال الأحداث         توثيق الأطر الرسمية
التبعيات المخفية               طوبولوجيا البنية التحتية             علاقات الاستيراد
                               أهداف KPI والمقاييس التجارية
                               خريطة أصحاب المصلحة ومسارات الموافقة
                               دفاتر تشغيل Ops ومسارات التصعيد
                               خارطة الطريق وتتبع المعالم
```

---

## كيف يعمل

### الخطوة 1: فحص وتصنيف المشروع

يستكشف الدلائل حتى العمق 3 ويصنّف تلقائياً بناءً على أنماط الملفات.

```
deployment.yaml + service.yaml  →  k8s-workload
values.yaml (Helm)              →  infra-component
package.json + *.tsx            →  frontend
go.mod                          →  backend-go
Dockerfile + CI config          →  cicd
...19 نوعاً يُكشف تلقائياً
```

### الخطوة 2: توليد السياق

يولّد ملفات المعرفة في `.ai-agents/context/` عبر **تحليل فعلي للكود** بناءً على الأنواع المكتشفة.

```
تم اكتشاف خدمة backend
  → فحص routes/controllers → توليد api-spec.json
  → فحص entities/schemas   → توليد data-model.md
  → فحص إعداد Kafka        → توليد event-spec.json
```

### الخطوة 3: توليد AGENTS.md

يولّد AGENTS.md لكل دليل باستخدام القوالب المناسبة.

```
Root AGENTS.md (الأعراف العالمية)
  → Commits: Conventional Commits
  → PR: قالب مطلوب، موافقة واحدة على الأقل
  → الفروع: feature/{ticket}-{desc}
       │
       ▼ موروث تلقائياً (لا يتكرر في العناصر الفرعية)
  apps/api/AGENTS.md
    → تجاوز فقط: "هذه الخدمة تستخدم Python"
```

تستخدم القواعد العالمية **نمط الوراثة** — اكتبها في مكان واحد، وتُطبَّق تلقائياً على التدرج.

```
Root AGENTS.md ──────────────────────────────────────────
│ الأعراف العالمية:
│  - Commits: Conventional Commits (feat:, fix:, chore:)
│  - PR: قالب مطلوب، مراجع واحد على الأقل
│  - الفرع: feature/{ticket}-{desc}
│
│     موروث تلقائياً           موروث تلقائياً
│     ┌──────────────────┐   ┌──────────────────┐
│     ▼                  │   ▼                  │
│  apps/api/AGENTS.md    │   infra/AGENTS.md    │
│  (قواعد إضافية فقط     │   (قواعد إضافية فقط  │
│   محددة)               │    محددة)            │
│  "هذه الخدمة تستخدم    │   "عند تغيير قيم Helm│
│   Python"              │    اسأل أولاً"        │
└────────────────────────┴───────────────────────
```

**الفوائد:**
- تريد تغيير قواعد commit؟ → عدِّل الجذر فقط
- تضيف خدمة جديدة؟ → تُطبَّق القواعد العالمية تلقائياً
- تحتاج قواعد مختلفة لخدمة معينة؟ → تجاوزها في AGENTS.md لتلك الخدمة

### الخطوة 4: Bootstrap خاص بالموردين

يضيف جسوراً إلى إعدادات خاصة بالموردين حتى **تقرأ جميع أدوات الذكاء الاصطناعي** AGENTS.md المولَّد.

```
┌──────────────┐     ┌─────────────┐     ┌─────────────┐
│ Claude Code  │     │   Cursor    │     │   Codex     │
│  CLAUDE.md   │     │  .mdc rules │     │  AGENTS.md  │
│      ↓       │     │      ↓      │     │  (native)   │
│ "اقرأ        │     │ "اقرأ       │     │      ✓      │
│  AGENTS.md"  │     │  AGENTS.md" │     │             │
└──────┬───────┘     └──────┬──────┘     └─────────────┘
       └──────────┬─────────┘
                  ▼
           AGENTS.md (مصدر الحقيقة الواحد)
                  │
        ┌─────────┼─────────┐
        ▼         ▼         ▼
  .ai-agents/  .ai-agents/  .ai-agents/
   context/     skills/      roles/
```

> **مبدأ:** تُولَّد ملفات Bootstrap فقط للموردين المستخدمين فعلاً. لا تُنشأ ملفات إعداد لأدوات غير مستخدمة.

---

## توافق الموردين

| الأداة | تقرأ AGENTS.md تلقائياً | Bootstrap |
|---|---|---|
| **OpenAI Codex** | نعم (أصلي) | غير مطلوب |
| **Claude Code** | جزئي (احتياطي) | يضيف توجيهاً إلى `CLAUDE.md` |
| **Cursor** | لا | يضيف `.mdc` إلى `.cursor/rules/` |
| **GitHub Copilot** | لا | يولّد `.github/copilot-instructions.md` |
| **Windsurf** | لا | يضيف توجيهاً إلى `.windsurfrules` |
| **Aider** | نعم | يضيف read إلى `.aider.conf.yml` |

توليد bootstraps تلقائياً:
```bash
bash scripts/sync-ai-rules.sh
```

---

## الهيكل المولَّد

```
project-root/
├── AGENTS.md                          # وكيل PM (التنسيق العام)
├── .ai-agents/
│   ├── context/                       # ملفات المعرفة (تُحمَّل عند بدء الجلسة)
│   │   ├── domain-overview.md         #   النطاق التجاري، السياسات، القيود
│   │   ├── data-model.md             #   تعريفات الكيانات، العلاقات، انتقالات الحالة
│   │   ├── api-spec.json              #   خريطة API (JSON DSL، توفير 3x في التوكنز)
│   │   ├── event-spec.json            #   مواصفات أحداث Kafka/MQ
│   │   ├── infra-spec.md              #   Helm charts، الشبكة، ترتيب النشر
│   │   ├── external-integration.md    #   APIs خارجية، المصادقة، حدود المعدل
│   │   ├── business-metrics.md        #   مؤشرات KPI وOKR، نموذج الإيرادات، معايير النجاح
│   │   ├── stakeholder-map.md         #   صانعو القرار، مسارات الموافقة، RACI
│   │   ├── ops-runbook.md             #   الإجراءات التشغيلية، التصعيد، SLA
│   │   └── planning-roadmap.md        #   المعالم، التبعيات، الجدول الزمني
│   ├── skills/                        # سير عمل سلوكية (تُحمَّل عند الطلب)
│   │   ├── develop/SKILL.md           #   التطوير: تحليل → تصميم → تنفيذ → اختبار → PR
│   │   ├── deploy/SKILL.md            #   النشر: وسم → طلب نشر → تحقق
│   │   ├── review/SKILL.md            #   المراجعة: قائمة تحقق
│   │   ├── hotfix/SKILL.md            #   سير عمل الإصلاح الطارئ
│   │   └── context-update/SKILL.md    #   إجراء تحديث ملفات السياق
│   └── roles/                         # تعريفات الأدوار (عمق سياق خاص بالدور)
│       ├── pm.md                      #   مدير المشروع
│       ├── backend.md                 #   مطور Backend
│       ├── frontend.md                #   مطور Frontend
│       ├── sre.md                     #   SRE / البنية التحتية
│       └── reviewer.md               #   مراجع الكود
│
├── apps/
│   ├── api/AGENTS.md                  # وكلاء لكل خدمة
│   └── web/AGENTS.md
└── infra/
    └── helm/AGENTS.md
```

---

## مشغّل الجلسات

```bash
./ai-agency.sh               # وضع اختيار تفاعلي
./ai-agency.sh --multi       # تشغيل وكلاء متعددين في tmux بالتوازي
./ai-agency.sh --agent pm    # تشغيل وكيل محدد مباشرة
./ai-agency.sh --tool codex  # تحديد أداة الذكاء الاصطناعي مسبقاً
→ Different agents work independently in each pane
→ Switch panes with Ctrl+B N
```

---

## تحسين التوكنز

| الصيغة | عدد التوكنز | ملاحظات |
|---|---|---|
| وصف API بلغة طبيعية | ~200 توكن | |
| JSON DSL | ~70 توكن | **توفير 3x** |

**مثال api-spec.json:**
```json
{
  "service": "order-api",
  "apis": [{
    "method": "POST",
    "path": "/api/v1/orders",
    "domains": ["Order", "Payment"],
    "sideEffects": ["kafka:order-created", "db:orders.insert"]
  }]
}
```

**هدف AGENTS.md:** أقل من **300 توكن** بعد الاستبدال

---

## بروتوكول استعادة الجلسة

```
بداية الجلسة:
  1. اقرأ AGENTS.md (معظم أدوات الذكاء الاصطناعي تفعل ذلك تلقائياً)
  2. اتبع مسارات ملفات السياق لتحميل .ai-agents/context/
  3. تحقق من .ai-agents/context/current-work.md (العمل الجاري)
  4. git log --oneline -10 (فهم التغييرات الأخيرة)

نهاية الجلسة:
  1. العمل الجاري → سجّله في current-work.md
  2. المعرفة المكتسبة حديثاً بالنطاق → حدِّث ملفات السياق
  3. المهام غير المكتملة → سجّلها صراحةً
```

---

## صيانة السياق

عند تغيير الكود، يجب تحديث ملفات `.ai-agents/context/` وفقاً لذلك.

```
إضافة/تغيير/حذف API       →  حدِّث api-spec.json
تغيير مخطط DB             →  حدِّث data-model.md
تغيير مواصفات الأحداث     →  حدِّث event-spec.json
تغيير سياسة الأعمال       →  حدِّث domain-overview.md
تغيير التكامل الخارجي     →  حدِّث external-integration.md
تغيير إعداد البنية التحتية →  حدِّث infra-spec.md
تغيير أهداف KPI/OKR       →  حدِّث business-metrics.md
تغيير هيكل الفريق          →  حدِّث stakeholder-map.md
تغيير إجراء تشغيلي         →  حدِّث ops-runbook.md
تغيير المعالم/خارطة الطريق  →  حدِّث planning-roadmap.md
```

> الفشل في التحديث يعني أن الجلسة التالية ستـ**عمل بسياق قديم**.

---

## ملخص التدفق العام

```
┌──────────────────────────────────────────────────────────────────┐
│  1. الإعداد الأولي (مرة واحدة)                                   │
│                                                                  │
│  شغّل ./setup.sh (يقرأ HOW_TO_AGENTS.md ويتولى الباقي)           │
│       │                                                          │
│       ▼                                                          │
│  يحلل الذكاء الاصطناعي هيكل المشروع                              │
│       │                                                          │
│       ▼                                                          │
│  ينشئ AGENTS.md في كل         ينظّم المعرفة في                    │
│  دليل                         .ai-agents/context/                │
│  (هوية الوكيل + قواعد          (مواصفات API والنماذج والأحداث)   │
│   + صلاحيات)                                                     │
│                                                                  │
│  يعرّف سير العمل في           يعرّف الأدوار في                     │
│  .ai-agents/skills/            .ai-agents/roles/                 │
│  (إجراءات التطوير والنشر       (Backend، Frontend، SRE)          │
│   والمراجعة)                                                     │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│  2. الاستخدام اليومي                                             │
│                                                                  │
│  شغّل ./ai-agency.sh                                              │
│       │                                                          │
│       ▼                                                          │
│  اختر الوكيل (PM؟ Backend؟ SRE؟)                                 │
│       │                                                          │
│       ▼                                                          │
│  اختر أداة الذكاء الاصطناعي (Claude؟ Codex؟ Cursor؟)             │
│       │                                                          │
│       ▼                                                          │
│  تبدأ الجلسة → يُحمَّل AGENTS.md تلقائياً → يُحمَّل                     │
│  .ai-agents/context/ → العمل!                                    │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│  3. الصيانة المستمرة                                             │
│                                                                  │
│  عند تغيير الكود:                                                │
│    - يحدّث الذكاء الاصطناعي .ai-agents/context/ تلقائياً           │
│    - أو يوجّه إنسان "هذا مهم، سجّله"                               │
│                                                                  │
│  عند إضافة خدمة جديدة:                                           │
│    - شغّل HOW_TO_AGENTS.md مجدداً →                                │
│      يُولَّد AGENTS.md جديد تلقائياً                                 │
│    - تُورَّث القواعد العالمية تلقائياً                               │
│                                                                  │
│  عندما يخطئ الذكاء الاصطناعي:                                    │
│    - "أعد تحليل هذا" → قدّم تلميحات → بمجرد الفهم،                │
│      حدِّث .ai-agents/context/                                     │
│    - هذه الحلقة التغذوية تحسّن جودة السياق                        │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## تشبيه: الفريق التقليدي مقابل فريق وكلاء الذكاء الاصطناعي

```
              الفريق التقليدي           فريق وكلاء الذكاء الاصطناعي
              ────────────────────       ──────────────────────────────
 القائد       PM (إنسان)                Root AGENTS.md (وكيل PM)
 الأعضاء      N مطور                   AGENTS.md في كل دليل
 التهيئة      Confluence/Notion         .ai-agents/context/
 الأدلة       ويكي الفريق              .ai-agents/skills/
 تعريف الأدوار مسمى الوظيفة/وثائق R&R  .ai-agents/roles/
 قواعد الفريق وثائق أعراف الفريق       الأعراف العالمية (موروثة)
 بدء العمل    الوصول للمكتب            تبدأ الجلسة → يُحمَّل AGENTS.md
 نهاية العمل  المغادرة (الذاكرة محفوظة)  تنتهي الجلسة (الذاكرة تُفقد!)
 اليوم التالي الذاكرة سليمة            يُحمَّل .ai-agents/context/ (الذاكرة تُستعاد)
```

**الفرق الرئيسي:** يحتفظ البشر بذاكرتهم بعد مغادرة العمل، لكن الذكاء الاصطناعي ينسى كل شيء في كل مرة.
لهذا يوجد `.ai-agents/context/` — يعمل كـ**ذاكرة طويلة المدى** للذكاء الاصطناعي.

---

## قائمة تحقق الاعتماد

```
المرحلة 1 (الأساسيات)          المرحلة 2 (السياق)               المرحلة 3 (العمليات)
──────────────────              ─────────────────                ────────────────────
☐ توليد AGENTS.md              ☐ إنشاء .ai-agents/context/      ☐ تعريف .ai-agents/roles/
☐ تسجيل أوامر build/test       ☐ domain-overview.md             ☐ تشغيل جلسات متعددة الوكلاء
☐ تسجيل الأعراف والقواعد       ☐ api-spec.json (DSL)            ☐ سير عمل .ai-agents/skills/
☐ الأعراف العالمية             ☐ data-model.md                  ☐ حلقة التغذية الراجعة التكرارية
☐ bootstraps الموردين           ☐ إعداد قواعد الصيانة
```

---

## المخرجات

| الملف | الجمهور | الغرض |
|---|---|---|
| `HOW_TO_AGENTS.md` | الذكاء الاصطناعي | دليل تعليمات meta يقرأه الوكلاء وينفذونه |
| `setup.sh` | الإنسان | إعداد تفاعلي: اكتشاف الأدوات + اختيار اللغة + التوليد الكامل |
| `README.md` | الإنسان | هذا الوثيق — دليل للفهم البشري |
| `ai-agency.sh` | الإنسان | اختيار الوكيل → مشغّل جلسة الذكاء الاصطناعي |
| `AGENTS.md` (كل دليل) | الذكاء الاصطناعي | هوية وقواعد الوكيل لكل دليل |
| `.ai-agents/context/*.md/json` | الذكاء الاصطناعي | معرفة النطاق منظَّمة مسبقاً |
| `.ai-agents/skills/*/SKILL.md` | الذكاء الاصطناعي | سير عمل موحّدة |
| `.ai-agents/roles/*.md` | الذكاء الاصطناعي/الإنسان | استراتيجيات تحميل السياق لكل دور |

---

## المراجع

- [Kurly OMS Team AI Workflow](https://helloworld.kurly.com/blog/oms-claude-ai-workflow/) — مصدر إلهام لتصميم السياق في هذا النظام
- [AGENTS.md Standard](https://agents.md/) — معيار تعليمات الوكلاء المحايد من الموردين
- [ETH Zurich Research](https://www.infoq.com/news/2026/03/agents-context-file-value-review/) — "وثِّق فقط ما لا يمكن استنتاجه"

---

## الرخصة

MIT

---

<p align="center">
  <sub>اجعل الوقت الذي يستغرقه وكلاء الذكاء الاصطناعي لفهم مشروعك يساوي صفراً.</sub>
</p>
